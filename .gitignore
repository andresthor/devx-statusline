# Python
__pycache__/
*.py[cod]

# macOS
.DS_Store

# Local config (per-machine, not checked in)
statusline/config.toml

# Captured statusline payloads (may contain internal repo names/paths)
debug-payload.json
